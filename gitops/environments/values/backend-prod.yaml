redis:
  enabled: false

replicaCount:
  backend: 1

image:
  backend:
    repository: ghcr.io/lucchmielowski/diiage-finals/backend
    tag: "6471f918325879257b3f1983a63a83fda534c99b"
    pullPolicy: Always

service:
  backend:
    type: ClusterIP
    port: 80

resources:
  limits:
    cpu: 200m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 64Mi

autoscaling:
  enabled: false

backend:
  config:
    appName: "shop-backend"
    logLevel: "debug"

